unittests:
  image: aictx/ci-python-arch:latest
  tags:
    - python
    - aiCTX

  before_script:
    - pip install --upgrade pip
    - pip install . --no-cache-dir
    - git clone https://aur.archlinux.org/nest.git 
    - cd nest
    - makepkg -sri
 
  script:
    - pytest 
